module Constants where

gWidth, gHeight :: Int
gWidth = 800
gHeight = 600

fWidth, fHeight :: (Num a) => a
fWidth = 800
fHeight = 600

fps :: (Num a) => a
fps = 60

gTitle :: String
gTitle = "Fasdjkl"

